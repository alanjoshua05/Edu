function mat() {
    var childPageContainer = document.getElementById("matc");
    // Toggle visibility of the child page container
    childPageContainer.style.display =
        childPageContainer.style.display === "none" ? "block" : "none";
}
function phy() {
    var childPageContainer = document.getElementById("phyc");
    // Toggle visibility of the child page container
    childPageContainer.style.display =
        childPageContainer.style.display === "none" ? "block" : "none";
}
function chem() {
    var childPageContainer = document.getElementById("chemc");
    // Toggle visibility of the child page container
    childPageContainer.style.display =
        childPageContainer.style.display === "none" ? "block" : "none";
}
function eng() {
    var childPageContainer = document.getElementById("engc");
    // Toggle visibility of the child page container
    childPageContainer.style.display =
        childPageContainer.style.display === "none" ? "block" : "none";
}
function c() {
    var childPageContainer = document.getElementById("cc");
    // Toggle visibility of the child page container
    childPageContainer.style.display =
        childPageContainer.style.display === "none" ? "block" : "none";
}
function beee() {
    var childPageContainer = document.getElementById("beec");
    // Toggle visibility of the child page container
    childPageContainer.style.display =
        childPageContainer.style.display === "none" ? "block" : "none";
}